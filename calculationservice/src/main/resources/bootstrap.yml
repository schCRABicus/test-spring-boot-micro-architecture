spring:
  application:
    name: calculation-service
  cloud:
    config:
      uri: http://localhost:8888

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://test-service-discovery:8761/eureka/